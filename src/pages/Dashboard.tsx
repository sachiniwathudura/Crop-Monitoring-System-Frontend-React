import "./Dashboard.css"

export function Dashboard() {

    //const customers = useSelector((state:Customer[]) => state);
    return (
        <>
            <div className="title">
            <h1 className="font-bold">Dashboard</h1>
            </div>

        </>
    );
}